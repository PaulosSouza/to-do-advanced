export enum I18NextCore {
  ResourceNotFound = 'core.resource-not-found',
  AlreadyExists = 'core.already-exists',
  AllUpdatedParamsUndefined = 'core.all-updated-values-undefined',
}

export enum I18NextUseCase {
  WrongCredentials = 'use-case.wrong-credentials',
}

export enum I18NextDomain {
  AtLeastOneSpecialCharacterPassword = 'domain.at-least-one-special-character-password',
  InvalidEmail = 'domain.invalid-email',
  InvalidNameLength = 'domain.invalid-name-length',
  InvalidPasswordLength = 'domain.invalid-password-length',
}
